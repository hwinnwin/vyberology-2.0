/**
 * A4 Dual Codegen
 *
 * Independent alternative code generation pipeline for LUMEN OS.
 * Must be architecturally independent from A3.
 * Owner: B-02 (A4 Owner)
 *
 * @module @vyberology/a4
 */

export { CodegenConfig, CodegenOutput, CodegenError } from './types';
export { generate } from './codegen';
